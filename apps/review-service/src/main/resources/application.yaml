spring:
  application:
    name: codesentinel-review-service

server:
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include: health,info

openai:
  api-key: ${OPENAI_API_KEY}